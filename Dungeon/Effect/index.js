
var Effect, compileListener;

module.exports = Effect = function(game, config){
  this.game = game;
  this.config = config;

  if(this.config.pre){
    for(var i in )
    this.pre =
  }

  if(this.config.mod){

  }

  if(this.config.post){

  }

};

Effect.prototype.applyToEffectable = function(effectable){

};

Effect.prototype.removeFromToEffectable = function(effectable){

};

compileListener = function(effect, key){

}